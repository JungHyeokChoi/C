#include <stdio.h>

int main() {

	char tmp = 'a';

	printf("%c \n\n", tmp - 32);

	/*
	printf("알파벳을 입력하시오 : ");
	scanf("%c", &tmp);

	if (tmp >= 65 && tmp < 97)
		tmp = tmp + 32;

	else if (tmp >= 97)
		tmp = tmp - 32;


	printf("입력한 알파벳의 소.대문자가 변경되어 나옵니다 : %c\n\n", tmp);
	*/

	return 0;
}